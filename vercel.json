{
  "buildCommand": "npm install -w apps/admin && cd apps/admin && npm run build",
  "outputDirectory": "apps/admin/dist",
  "installCommand": "npm install --workspaces=false",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
